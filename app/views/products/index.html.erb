
<h1>All Non-prepped Products</h1>

<a href="/globals/products/new"><button type="button" class="btn btn-success">Add New Product</button></a>
<br>
<br>
  <table class="table table-striped table-responsive">
    <thead>
      <tr>
        <th class="sticky2">Name</th>
        <th class="sticky2">Distributor </th>
        <th class="sticky2">Measurement</th>
        <th class="sticky2">Item Type</th>
        <th class="sticky2">Case Quantity </th>
        <th class="sticky2">Price</th>
       	<th class="sticky2">Markup</th>
        <th class="sticky2">Markup Price </th>
        <th class="sticky2">Prepped </th>
        <th class="sticky2">Edit</th>
        <th class="sticky2">Delete</th>
      </tr>
    </thead>
    <tbody>

	<% @products.each do |product| %>
	  <tr>
        <td><%= product.name %></td>
		    <td><%= product.distributor %></td>
      	<td><%= product.measurement %></td> 
      	<td><%= product.item_type %> </td>
      	<td><%= product.case_quantity %> </td>	
      	<td><%= number_to_currency(product.price) %> </td>
        <td><%= number_to_percentage(product.markup, precision: 1) %> </td>
        <td><%= number_to_currency( (product.price) * ((product.markup * 0.01)+1)) %> </td>

        <td><%= link_to 'Add', "/globals/products/prepcenter/#{product.id}/new", class: "btn btn-success"%></td>

	      <td><%= link_to 'Edit', "/globals/products/#{product.id}/edit", class: "btn btn-primary"%></td>
        <td><%= button_to "Delete", { action: "destroy", id: product.id }, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger" %> </td>
	 </tr>
	<% end %>

    </tbody>
  </table>

